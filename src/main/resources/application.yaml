spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/file_uploader_db
    username: rayandev
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

aws:
  s3:
    bucket: fs-rayandev-customer-test
    region: us-east-2